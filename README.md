# ogit-python-library

## Toolchain

* Environment 
  pipenv
* Dependency Management 
  pipenv `pipenv install --dev`
* Version Management
  setuptools_scm
* Build 
  wheel `$(pipenv --py) -m pep517.build .`
* Publish 
  TODO TBD